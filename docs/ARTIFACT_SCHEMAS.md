# Artifact Schemas

Initial schema placeholders live under `src/radjax_contract/schemas`.

The first scaffold records schema names and versions only. Production schema
validation will be added in migration slices after compatibility shims exist.

