# Architecture

RADJAX-Contract is the shared artifact boundary package for RADJAX-Tome and
RADJAX-Student.

It owns schema names, manifest models, vocabulary contracts, provenance hashes,
and validation entrypoints. It intentionally avoids teacher and student runtime
dependencies.

